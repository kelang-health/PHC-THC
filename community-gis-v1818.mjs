const VERSION='1.8.18';
const DEFAULT_CENTER=[18.2696,99.5071];
let supabase=null,observer=null,map=null,markerLayer=null,draftMarker=null,workspace=null;
let houses=[],volunteerNames=new Map(),selectedHouse=null,draft=null,leafletPromise=null,enhanceTimer=null;
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const hasCoord=h=>Number.isFinite(Number(h?.latitude))&&Number.isFinite(Number(h?.longitude));
const fmtCoord=(lat,lng)=>`${Number(lat).toFixed(6)}, ${Number(lng).toFixed(6)}`;
const natural=(a,b)=>String(a.house_no||'').localeCompare(String(b.house_no||''),'th',{numeric:true,sensitivity:'base'});

function injectStyle(){
  document.getElementById('community-gis-style')?.remove();
  const s=document.createElement('style');s.id='community-gis-style';s.textContent=`
  .community-gis-action{background:#0b6f60!important;color:#fff!important;border-color:#0b6f60!important}.community-gis-view{display:grid;gap:16px;margin-top:14px}.community-gis-view[hidden]{display:none!important}
  .community-gis-head{display:flex;justify-content:space-between;align-items:flex-start;gap:14px;flex-wrap:wrap}.community-gis-head h4{margin:0 0 5px;font-size:1.24rem}.community-gis-head p{margin:0;color:#5d716c;line-height:1.55}
  .community-gis-tools{display:grid;grid-template-columns:1fr 1.35fr .75fr;gap:10px;align-items:end;background:#f5faf7;border:1px solid #d5e6df;border-radius:17px;padding:14px}.community-gis-tools label{margin:0;font-size:1rem}.community-gis-tools input,.community-gis-tools select{width:100%;min-height:56px;padding:11px 13px;border:2px solid #bad2c8;border-radius:13px;background:#fff;font:inherit;font-size:1.06rem;font-weight:750;color:#17312d}.community-gis-tools input:focus,.community-gis-tools select:focus{outline:3px solid #9fcfc1;outline-offset:1px}.community-gis-count{display:flex;align-items:center;justify-content:center;min-height:56px;padding:10px 12px;border-radius:13px;background:#fff;border:1px solid #d6e5df;font-weight:850;text-align:center}
  .community-gis-help{padding:13px 15px;border-radius:14px;background:#fff8df;border:1px solid #eadca9;color:#604f1e;font-size:.94rem;line-height:1.6}.community-gis-map{height:430px;min-height:340px;border:2px solid #c8ddd4;border-radius:18px;overflow:hidden;background:#e9f0ed}.community-gis-legend{display:flex;gap:14px;flex-wrap:wrap;color:#51645f;font-size:.84rem}.community-gis-legend span{display:inline-flex;align-items:center;gap:6px}.community-gis-dot{width:13px;height:13px;border-radius:50%;border:2px solid #fff;box-shadow:0 0 0 1px #91a49d}.community-gis-dot.ok{background:#17745f}.community-gis-dot.review{background:#b75a42}.community-gis-dot.other{background:#3979a8}
  .community-house-card{display:grid;gap:11px;padding:16px;border-radius:17px;background:#fff;border:1px solid #d7e6e0;box-shadow:0 8px 22px #17312d0d}.community-house-card[hidden]{display:none!important}.community-house-head{display:flex;justify-content:space-between;align-items:flex-start;gap:12px;flex-wrap:wrap}.community-house-head strong{font-size:1.25rem}.community-house-meta{color:#5b716a;line-height:1.6}.community-house-coord{font-family:ui-monospace,SFMono-Regular,Menlo,monospace;background:#edf5f2;padding:11px 12px;border-radius:11px;word-break:break-word}.community-house-actions{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}
  .community-gis-btn,.community-gis-link{display:flex;align-items:center;justify-content:center;text-align:center;min-height:60px;padding:12px 14px;border:2px solid #b9d1c7;border-radius:14px;background:#fff;color:#194f46;font:inherit;font-size:1.04rem;font-weight:850;line-height:1.3;text-decoration:none;cursor:pointer}.community-gis-btn:hover,.community-gis-link:hover{background:#f1f8f5}.community-gis-btn.gps{background:#e9f5ff;color:#164c77;border-color:#b8d9f0}.community-gis-btn.save{background:#0b6f60;color:#fff;border-color:#0b6f60}.community-gis-btn:disabled,.community-gis-link.disabled{opacity:.45;pointer-events:none;cursor:not-allowed}.community-gis-message{min-height:1.5em;font-weight:800;line-height:1.5}.community-gis-message.good{color:#116450}.community-gis-message.bad{color:#a23e31}.community-gis-message.info{color:#315c72}
  .community-gis-confirm{position:fixed;inset:0;z-index:3000;background:#102e287d;display:flex;align-items:center;justify-content:center;padding:18px}.community-gis-confirm[hidden]{display:none!important}.community-gis-confirm-card{width:min(520px,100%);background:#fff;border-radius:20px;padding:22px;box-shadow:0 20px 60px #0004}.community-gis-confirm-card h3{margin:0 0 8px;font-size:1.38rem}.community-gis-confirm-card p{color:#536a64;line-height:1.65}.community-gis-confirm-actions{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-top:16px}.community-gis-confirm-actions button{min-height:60px;border:0;border-radius:14px;font:inherit;font-size:1.05rem;font-weight:850;cursor:pointer}.community-gis-cancel{background:#e8f0ed;color:#294b44}.community-gis-confirm-save{background:#0b6f60;color:#fff}.leaflet-popup-content{font-family:inherit;font-size:15px;line-height:1.55}
  @media(max-width:760px){.community-gis-tools{grid-template-columns:1fr}.community-gis-map{height:52vh;min-height:360px}.community-house-actions{grid-template-columns:1fr}.community-gis-btn,.community-gis-link{min-height:64px;font-size:1.1rem}.community-gis-tools input,.community-gis-tools select{min-height:60px;font-size:1.1rem}.community-gis-confirm-actions{grid-template-columns:1fr}.community-gis-confirm-actions button{min-height:64px;font-size:1.1rem}.community-gis-help{font-size:1rem}}
  `;document.head.appendChild(s);
}

function setVersion(){const f=document.querySelector('.login-version');if(f)f.textContent=`Cloud v${VERSION}`;}
async function ensureLeaflet(){
  if(window.L)return window.L;if(leafletPromise)return leafletPromise;
  leafletPromise=new Promise((resolve,reject)=>{
    if(!document.querySelector('link[data-phc-leaflet]')){const l=document.createElement('link');l.rel='stylesheet';l.href='https://unpkg.com/leaflet@1.9.4/dist/leaflet.css';l.dataset.phcLeaflet='1';document.head.appendChild(l);}
    let sc=document.querySelector('script[data-phc-leaflet]');if(sc){if(window.L)return resolve(window.L);sc.addEventListener('load',()=>resolve(window.L),{once:true});sc.addEventListener('error',reject,{once:true});return;}
    sc=document.createElement('script');sc.src='https://unpkg.com/leaflet@1.9.4/dist/leaflet.js';sc.async=true;sc.dataset.phcLeaflet='1';sc.onload=()=>resolve(window.L);sc.onerror=()=>reject(new Error('ไม่สามารถโหลด Leaflet ได้'));document.head.appendChild(sc);
  });return leafletPromise;
}

async function fetchData(community){
  const all=[];for(let from=0;;from+=1000){const {data,error}=await supabase.from('houses').select('id,hcode,house_no,moo,community,latitude,longitude,coordinate_source,coordinate_status,record_status,review_required,review_reason,volunteer_pid').eq('community',community).order('house_no').range(from,from+999);if(error)throw error;all.push(...(data||[]));if(!data||data.length<1000)break;}
  houses=all.sort(natural);volunteerNames=new Map();
  const {data:vols}=await supabase.from('volunteers').select('source_pid,display_name,community,active').eq('community',community).eq('active',true);
  (vols||[]).forEach(v=>volunteerNames.set(String(v.source_pid),v.display_name));
}
function volunteerName(h){return h?.volunteer_pid==null?'ยังไม่ได้มอบหมาย':volunteerNames.get(String(h.volunteer_pid))||'อสม. ในพื้นที่';}
function statusText(h){if(!hasCoord(h))return 'ยังไม่มีพิกัด';if(h.review_required)return 'มีพิกัด · ควรตรวจสอบ';if(String(h.coordinate_status||'').startsWith('resolved_'))return 'ยืนยันพิกัดจากพื้นที่แล้ว';return 'มีพิกัด';}
function markerColor(h){if(h.review_required)return '#b75a42';if(String(h.coordinate_status||'').startsWith('resolved_'))return '#17745f';return '#3979a8';}
function setMessage(text,type='info'){const e=workspace?.querySelector('[data-gis-message]');if(e){e.textContent=text||'';e.className=`community-gis-message ${type}`;}}
function mapLinks(){
  const view=workspace?.querySelector('[data-gis-view-map]'),dir=workspace?.querySelector('[data-gis-dir]');if(!view||!dir)return;
  const p=draft||(hasCoord(selectedHouse)?{lat:Number(selectedHouse.latitude),lng:Number(selectedHouse.longitude)}:null);
  if(!p){view.classList.add('disabled');dir.classList.add('disabled');view.removeAttribute('href');dir.removeAttribute('href');return;}
  const q=`${p.lat},${p.lng}`;view.href=`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(q)}`;dir.href=`https://www.google.com/maps/dir/?api=1&destination=${encodeURIComponent(q)}`;view.classList.remove('disabled');dir.classList.remove('disabled');
}
function updateCard(){
  const card=workspace?.querySelector('[data-gis-house-card]');if(!card)return;if(!selectedHouse){card.hidden=true;return;}card.hidden=false;
  card.querySelector('[data-gis-house-title]').textContent=`บ้าน ${selectedHouse.house_no||'ไม่ระบุ'}`;
  card.querySelector('[data-gis-house-status]').textContent=statusText(selectedHouse);
  card.querySelector('[data-gis-house-meta]').textContent=`หมู่ ${selectedHouse.moo||'—'} · ${selectedHouse.community||'—'} · ผู้รับผิดชอบ: ${volunteerName(selectedHouse)} · HCODE ${selectedHouse.hcode||'—'}`;
  const p=draft||(hasCoord(selectedHouse)?{lat:Number(selectedHouse.latitude),lng:Number(selectedHouse.longitude)}:null);
  card.querySelector('[data-gis-coord]').textContent=p?`${draft?'พิกัดใหม่ที่เลือก':'พิกัดปัจจุบัน'}: ${fmtCoord(p.lat,p.lng)}`:'บ้านหลังนี้ยังไม่มีพิกัด — ใช้ GPS หรือแตะแผนที่เพื่อเลือกตำแหน่ง';
  card.querySelector('[data-gis-save]').disabled=!draft;card.querySelector('[data-gis-reset]').disabled=!draft;mapLinks();
}
function renderCount(){const e=workspace?.querySelector('[data-gis-count]');if(e){const mapped=houses.filter(hasCoord).length;e.textContent=`มีพิกัด ${mapped.toLocaleString('th-TH')} / ${houses.length.toLocaleString('th-TH')} หลัง`;}}
function addBaseLayers(L){const road=L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:20,attribution:'&copy; OpenStreetMap'}).addTo(map);const sat=L.tileLayer('https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}',{maxZoom:20,attribution:'Tiles &copy; Esri'});const labels=L.tileLayer('https://services.arcgisonline.com/ArcGIS/rest/services/Reference/World_Boundaries_and_Places/MapServer/tile/{z}/{y}/{x}',{maxZoom:20});L.control.layers({'แผนที่ถนน':road,'ภาพถ่ายดาวเทียม':L.layerGroup([sat,labels])},null,{collapsed:true}).addTo(map);}
function renderMarkers(){
  if(!map||!markerLayer)return;markerLayer.clearLayers();const bounds=[];
  houses.forEach(h=>{if(!hasCoord(h))return;const lat=Number(h.latitude),lng=Number(h.longitude);const m=window.L.circleMarker([lat,lng],{radius:8,color:'#fff',weight:2,fillColor:markerColor(h),fillOpacity:.92,bubblingMouseEvents:false}).addTo(markerLayer);m.bindPopup(`<strong>บ้าน ${esc(h.house_no)}</strong><br>หมู่ ${esc(h.moo)} · ${esc(h.community)}<br>อสม.: ${esc(volunteerName(h))}<br>${esc(statusText(h))}`);m.on('click',()=>selectHouse(h.id,true));bounds.push([lat,lng]);});
  if(bounds.length&&!selectedHouse){try{map.fitBounds(bounds,{padding:[24,24],maxZoom:17});}catch{}}
}
function renderHouseOptions(term=''){
  const select=workspace?.querySelector('[data-gis-house-select]'),count=workspace?.querySelector('[data-gis-filter-count]');if(!select)return;
  const q=String(term||'').trim().toLowerCase();const filtered=q?houses.filter(h=>String(h.house_no||'').toLowerCase().includes(q)||String(h.hcode||'').toLowerCase().includes(q)):houses;
  const keep=selectedHouse?.id?String(selectedHouse.id):'';select.innerHTML='<option value="">— แตะเพื่อเลือกบ้าน —</option>'+filtered.map(h=>`<option value="${esc(h.id)}">บ้าน ${esc(h.house_no||'ไม่ระบุ')} · หมู่ ${esc(h.moo||'—')}${hasCoord(h)?' · มีพิกัด':' · ไม่มีพิกัด'}</option>`).join('');if(keep&&filtered.some(h=>String(h.id)===keep))select.value=keep;if(count)count.textContent=`พบ ${filtered.length.toLocaleString('th-TH')} หลัง`;
}
function clearDraft(){draft=null;if(draftMarker&&map){map.removeLayer(draftMarker);draftMarker=null;}updateCard();if(selectedHouse&&hasCoord(selectedHouse))map.setView([Number(selectedHouse.latitude),Number(selectedHouse.longitude)],18);setMessage('ยกเลิกพิกัดใหม่แล้ว','info');}
function setDraft(lat,lng,source){
  if(!selectedHouse){setMessage('กรุณาเลือกบ้านก่อนกำหนดพิกัด','bad');return;}draft={lat:Number(lat),lng:Number(lng),source};if(draftMarker&&map)map.removeLayer(draftMarker);draftMarker=window.L.marker([draft.lat,draft.lng],{draggable:true}).addTo(map).bindPopup('พิกัดใหม่ที่กำลังจะบันทึก').openPopup();draftMarker.on('dragend',e=>{const p=e.target.getLatLng();draft.lat=p.lat;draft.lng=p.lng;updateCard();setMessage('เลื่อนหมุดแล้ว กรุณาตรวจตำแหน่งก่อนบันทึก','info');});map.setView([draft.lat,draft.lng],18);updateCard();setMessage(source==='gps'?'ได้ตำแหน่ง GPS แล้ว กรุณาตรวจจุดบนแผนที่':'เลือกจุดบนแผนที่แล้ว กรุณาตรวจตำแหน่งก่อนบันทึก','info');
}
function selectHouse(id,fromMarker=false){selectedHouse=houses.find(h=>String(h.id)===String(id))||null;if(!selectedHouse)return;draft=null;if(draftMarker&&map){map.removeLayer(draftMarker);draftMarker=null;}const s=workspace.querySelector('[data-gis-house-select]');if(s)s.value=String(selectedHouse.id);updateCard();if(hasCoord(selectedHouse)){map.setView([Number(selectedHouse.latitude),Number(selectedHouse.longitude)],18);setMessage(fromMarker?`เลือกบ้าน ${selectedHouse.house_no} จากแผนที่แล้ว`:`แสดงบ้าน ${selectedHouse.house_no} แล้ว สามารถดูเส้นทางหรือกำหนดพิกัดใหม่ได้`,'good');}else setMessage(`บ้าน ${selectedHouse.house_no} ยังไม่มีพิกัด ใช้ GPS หรือแตะตำแหน่งบนแผนที่`,'info');}
function captureGps(){if(!selectedHouse){setMessage('กรุณาเลือกบ้านก่อนกดใช้ GPS','bad');return;}if(!navigator.geolocation){setMessage('อุปกรณ์นี้ไม่รองรับ GPS','bad');return;}const b=workspace.querySelector('[data-gis-gps]'),old=b.textContent;b.disabled=true;b.textContent='กำลังจับตำแหน่ง…';navigator.geolocation.getCurrentPosition(p=>{b.disabled=false;b.textContent=old;setDraft(p.coords.latitude,p.coords.longitude,'gps');},e=>{b.disabled=false;b.textContent=old;setMessage(e.code===1?'ยังไม่ได้อนุญาต Location กรุณาอนุญาตแล้วลองใหม่':e.code===2?'ยังหาตำแหน่งไม่ได้ กรุณาออกพื้นที่โล่งแล้วลองใหม่':'จับตำแหน่งไม่ทันเวลา กรุณาลองใหม่','bad');},{enableHighAccuracy:true,timeout:15000,maximumAge:0});}
function openConfirm(){if(!selectedHouse||!draft)return;const d=workspace.querySelector('[data-gis-confirm]');d.querySelector('[data-confirm-house]').textContent=`บ้าน ${selectedHouse.house_no||'ไม่ระบุ'} หมู่ ${selectedHouse.moo||'—'}`;d.querySelector('[data-confirm-coord]').textContent=fmtCoord(draft.lat,draft.lng);d.hidden=false;d.querySelector('[data-confirm-save]').focus();}
function closeConfirm(){const d=workspace?.querySelector('[data-gis-confirm]');if(d)d.hidden=true;}
async function saveDraft(){
  if(!selectedHouse||!draft)return;const b=workspace.querySelector('[data-confirm-save]'),old=b.textContent;b.disabled=true;b.textContent='กำลังบันทึก…';
  try{const {data,error}=await supabase.rpc('update_house_coordinates',{p_house_id:selectedHouse.id,p_latitude:draft.lat,p_longitude:draft.lng,p_source:draft.source});if(error)throw error;selectedHouse.latitude=Number(data?.latitude??draft.lat);selectedHouse.longitude=Number(data?.longitude??draft.lng);selectedHouse.coordinate_source=data?.coordinate_source||draft.source;selectedHouse.coordinate_status=data?.coordinate_status||selectedHouse.coordinate_status;selectedHouse.review_required=false;selectedHouse.review_reason='';draft=null;if(draftMarker&&map){map.removeLayer(draftMarker);draftMarker=null;}closeConfirm();renderMarkers();renderCount();renderHouseOptions(workspace.querySelector('[data-gis-search]')?.value||'');updateCard();map.setView([Number(selectedHouse.latitude),Number(selectedHouse.longitude)],18);setMessage(`บันทึกพิกัดบ้าน ${selectedHouse.house_no} เรียบร้อยแล้ว`,'good');}
  catch(e){const m=String(e?.message||e);closeConfirm();setMessage(m.includes('HOUSE_OUT_OF_SCOPE')?'บัญชีนี้ไม่มีสิทธิ์แก้พิกัดบ้านหลังนี้':m.includes('INVALID_COORDINATES')?'พิกัดอยู่นอกช่วงประเทศไทย กรุณาตรวจสอบ':'บันทึกพิกัดไม่สำเร็จ กรุณาลองใหม่','bad');}
  finally{b.disabled=false;b.textContent=old;}
}

function buildSection(community){
  const sec=document.createElement('section');sec.className='community-view community-gis-view';sec.dataset.communityView='map';sec.dataset.communityGisSection='1';sec.hidden=true;sec.innerHTML=`
  <div class="community-gis-head"><div><h4>แผนที่บ้านในชุมชน</h4><p>เลือกบ้าน ดูพิกัดเดิม จับ GPS หรือตั้งจุดใหม่บนแผนที่ได้จากหน้าจอเดียว</p></div><strong class="community-gis-count" data-gis-count>กำลังโหลด…</strong></div>
  <div class="community-gis-tools"><label>ค้นหาบ้านเลขที่<input type="search" inputmode="text" data-gis-search placeholder="เช่น 123 หรือ HCODE"></label><label>เลือกบ้าน<select data-gis-house-select aria-label="เลือกบ้าน"><option>กำลังโหลดบ้าน…</option></select></label><div class="community-gis-count" data-gis-filter-count>—</div></div>
  <div class="community-gis-help"><strong>ใช้งาน 4 ขั้น:</strong> 1. เลือกบ้าน 2. ตรวจจุดเดิมบนแผนที่ 3. ถ้าต้องแก้ ให้กด “ใช้ตำแหน่งปัจจุบัน (GPS)” หรือแตะแผนที่ 4. กด “บันทึกพิกัดนี้” แล้วกดยืนยันอีกครั้ง</div>
  <div class="community-gis-map" data-gis-map aria-label="แผนที่บ้านในชุมชน"></div>
  <div class="community-gis-legend"><span><i class="community-gis-dot ok"></i>ยืนยันจากพื้นที่</span><span><i class="community-gis-dot review"></i>ควรตรวจสอบ</span><span><i class="community-gis-dot other"></i>พิกัดเดิม</span></div>
  <div class="community-house-card" data-gis-house-card hidden><div class="community-house-head"><strong data-gis-house-title></strong><span class="badge" data-gis-house-status></span></div><div class="community-house-meta" data-gis-house-meta></div><div class="community-house-coord" data-gis-coord></div><div class="community-house-actions"><button type="button" class="community-gis-btn gps" data-gis-gps>ใช้ตำแหน่งปัจจุบัน (GPS)</button><button type="button" class="community-gis-btn" data-gis-reset disabled>ยกเลิกพิกัดใหม่</button><a class="community-gis-link disabled" data-gis-view-map target="_blank" rel="noopener noreferrer">เปิดใน Google Maps</a><a class="community-gis-link disabled" data-gis-dir target="_blank" rel="noopener noreferrer">นำทางไปบ้านหลังนี้</a><button type="button" class="community-gis-btn save" data-gis-save disabled>บันทึกพิกัดนี้</button></div></div>
  <div class="community-gis-message info" data-gis-message aria-live="polite"></div>
  <div class="community-gis-confirm" data-gis-confirm hidden role="dialog" aria-modal="true" aria-labelledby="gis-confirm-title"><div class="community-gis-confirm-card"><h3 id="gis-confirm-title">ยืนยันบันทึกพิกัด</h3><p>กำลังบันทึก <strong data-confirm-house></strong><br>พิกัด <strong data-confirm-coord></strong><br>กรุณาตรวจว่าบ้านที่เลือกและตำแหน่งบนแผนที่ถูกต้อง</p><div class="community-gis-confirm-actions"><button type="button" class="community-gis-cancel" data-confirm-cancel>กลับไปตรวจสอบ</button><button type="button" class="community-gis-confirm-save" data-confirm-save>ยืนยันบันทึก</button></div></div></div>`;
  sec.querySelector('[data-gis-search]').oninput=e=>renderHouseOptions(e.target.value);sec.querySelector('[data-gis-house-select]').onchange=e=>e.target.value?selectHouse(e.target.value):(()=>{selectedHouse=null;clearDraft();updateCard();})();sec.querySelector('[data-gis-gps]').onclick=captureGps;sec.querySelector('[data-gis-reset]').onclick=clearDraft;sec.querySelector('[data-gis-save]').onclick=openConfirm;sec.querySelector('[data-confirm-cancel]').onclick=closeConfirm;sec.querySelector('[data-confirm-save]').onclick=saveDraft;sec.querySelector('[data-gis-confirm]').onclick=e=>{if(e.target===e.currentTarget)closeConfirm();};return sec;
}
async function openMap(community,sec){
  if(sec.dataset.loaded==='1'){setTimeout(()=>map?.invalidateSize(),80);return;}setMessage('กำลังโหลดบ้านและแผนที่…','info');
  try{await fetchData(community);renderHouseOptions();renderCount();const L=await ensureLeaflet();if(map){try{map.remove();}catch{}map=null;}map=L.map(sec.querySelector('[data-gis-map]')).setView(DEFAULT_CENTER,14);addBaseLayers(L);markerLayer=L.layerGroup().addTo(map);map.on('click',e=>{if(!selectedHouse){setMessage('กรุณาเลือกบ้านก่อนแตะตำแหน่งบนแผนที่','bad');return;}setDraft(e.latlng.lat,e.latlng.lng,'map');});renderMarkers();sec.dataset.loaded='1';setMessage(houses.length?`พบ ${houses.length.toLocaleString('th-TH')} หลังในสิทธิ์ของคุณ`:'ไม่พบบ้านในขอบเขตสิทธิ์','info');setTimeout(()=>map.invalidateSize(),100);}catch(e){setMessage(`เปิดแผนที่ไม่สำเร็จ: ${e?.message||e}`,'bad');}
}
function enhanceWorkspace(){
  const w=document.querySelector('#community-workspace');if(!w||w.hidden)return;const actions=w.querySelector('.community-actions'),head=w.querySelector('.community-workspace-head h3');if(!actions||!head)return;const community=head.textContent.trim();if(!community)return;
  workspace=w;w.dataset.communityGisReady='1';const existing=w.querySelector('[data-community-gis-section]');if(existing&&existing.dataset.communityName===community)return;
  w.querySelectorAll('.community-gis-action,[data-community-gis-section]').forEach(x=>x.remove());if(map){try{map.remove();}catch{}map=null;}houses=[];selectedHouse=null;draft=null;
  const btn=document.createElement('button');btn.type='button';btn.className='community-gis-action';btn.dataset.communityAction='map';btn.innerHTML='<span>⌖</span><strong>แผนที่บ้าน</strong><small>เลือกบ้าน · GPS · นำทาง</small>';actions.appendChild(btn);
  const sec=buildSection(community);sec.dataset.communityName=community;actions.insertAdjacentElement('afterend',sec);
  btn.onclick=async()=>{workspace=w;w.querySelectorAll('.community-actions button').forEach(x=>x.classList.toggle('active',x===btn));w.querySelectorAll('[data-community-view]').forEach(x=>x.hidden=x!==sec);await openMap(community,sec);sec.scrollIntoView({behavior:'smooth',block:'start'});};
}
function startObserver(){const w=document.querySelector('#community-workspace');if(!w)return;observer?.disconnect();observer=new MutationObserver(()=>{clearTimeout(enhanceTimer);enhanceTimer=setTimeout(enhanceWorkspace,20);});observer.observe(w,{childList:true,subtree:true,attributes:true,attributeFilter:['hidden']});enhanceWorkspace();}
export async function initCommunityGIS(url,key){
  window.__PHC_COMMUNITY_GIS_1817__=true;if(window.__PHC_COMMUNITY_GIS_1818__)return;window.__PHC_COMMUNITY_GIS_1818__=true;injectStyle();setVersion();const {createClient}=await import('https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm');supabase=createClient(url,key,{auth:{persistSession:true,autoRefreshToken:false,detectSessionInUrl:false}});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',startObserver,{once:true});else startObserver();
}
