# Data API / Cloud→Local next-phase evidence — 25 September 2026

## Decision and blast radius
All work in this continuation was read-only on Production and Local migration/source files, metadata-only in the restricted Local governance directory, or isolated synthetic PostgreSQL/PostgREST CI. No active frontend/backend/line-auth/sync/JHCIS change, Cloud data export, Cloud deletion, production grant/RLS/migration edit, Production load test, Supabase branch, or PR merge. The OSM-PHC Cloud event/booking remains paused.

## 1. Off-device encrypted backup
The prior separately encrypted archive file holding 10,474 archived Local records remains in the restricted Local private directory and was fully restored in a fresh process disconnected from DB in the previous phase. This continuation found only ONE authorized connected remote computer. No verified separately approved backup target/second machine is connected or authorized. C: and D: on the same computer, and unapproved network/cloud folders, do NOT satisfy independent-device disaster recovery. No personal/health record was transmitted to a different endpoint in this phase.

**Off-device backup and restore: BLOCKED.** Keep the two original encryption keys and existing encrypted file private. Do not rotate or replace credentials to make the old unreadable file appear recoverable. A separately approved off-device storage and offline restore test remain mandatory for any irreversible Cloud data-reduction rollout.

## 2. Historical migration provenance (no real SQL copied to GitHub)
Read-only compared live PHC `supabase_migrations.schema_migrations` inventory (191 recorded versions) with actual local current and backup SQL filenames, and computed only private metadata/file-body hashes:
- 121 current local SQL files, 139 SQL files in the inspected backup subtree.
- 101 recorded versions have a same-version file candidate; **90 recorded versions have no exact-version SQL candidate** in these inspected locations.
- **51 recorded versions** have multiple non-placeholder SQL source variants with different normalized text (BOM, newline and trailing-whitespace normalization did not remove the difference). This is a file-provenance mismatch; it is NOT proof the live database was modified incorrectly or that the variations change SQL semantics.
- Nine of the 90 missing-version entries have a same-name file under a different version number. Renaming, repurposing or replaying these files blindly could corrupt migration order.
- Missing recorded versions by recorded date: 13 Sep 3, 15 Sep 2, 17 Sep 2, 19 Sep 10, 20 Sep 2, 21 Sep 5, 22 Sep 15, 23 Sep 42, 24 Sep 9.

Created `D:\\AppServ\\private\\osm-phc\\governance\\data-api-migration-provenance-20260925.json` (85,243 bytes) *on the authorized Local machine, outside web root*, recording inventory, version status, candidate relative paths, content SHA-256 hashes and same-name version aliases. The manifest contains **NO SQL payload, actual patient record, credentials or encryption key**. No file variant is auto-declared canonical: `canonical_source_confirmed=false` for every recorded version. The document/manifest are provenance aids, not a source restoration or SQL replay.

**Full historical replay: BLOCKED.** Do not mark existing CI deployment readiness green or run 101 partial files against a real database as a substitute for a complete, vetted, ordered historical source.

## 3. Actual RPC metadata review and stricter isolated negative case
Read-only reviewed current live function definitions: `my_household_cards_v1860` uses authenticated current volunteer ID to select verified, non-superseded assigned houses. `staff_household_cards_v2072` checks the signed-in staff profile and requested community in the same moo, and indicates own-community manage versus different-community read-only. This is source-code review of selected routines, NOT authenticated USER/STAFF/ADMIN end-to-end authorization testing and NOT certification of every dependent RPC.

The earlier fake member/house PostgREST fixture trusted the member's denormalized community and volunteer fields; this could conceal a stale-member-metadata scope error in the *test*. Updated `tests/phase4-cloud-local-scoped-http.sql` so synthetic member visibility is gated by the referenced house's RLS. Added a member whose own scope metadata is misleading and negative HTTP cases proving staff and volunteer roles cannot see it through the out-of-scope house. The new test uses **fictional IDs/data and disposable JWTs**. The updated CI run `36077594297` succeeded in all three offline jobs; the synthetic PostgREST fixture completed 96 timed calls, p95 4.23 ms *inside disposable CI only*. Do not assert an actual Production vulnerability or real-web latency improvement from this fixture.

Verified one public HEAD request to the current GitHub Pages PHC-THC landing URL returned HTTP 200; a HEAD check cannot validate authenticated UX, current screens, LINE login, app cache or p95 page timings.

## 4. Storage/performance release gate
No Cloud record was removed. Cloud storage reclaimed by this work **0 bytes**. Read-only status and previous cumulative SQL statement timing remain baselines only, not pre/post performance proof. Existing Cloud `health_persons` and `houses` are FK anchors with RESTRICT and CASCADE paths; current health worklist, target and report-cache functions rely on live Cloud table data. No mobile client should make an additional direct request to Local/private services to compensate for deleting current Cloud data.

**Next required gates**: (1) authorized protected off-device encrypted backup + standalone recovery on second device; (2) source provenance and correct SQL contents for all 191 histories, data-migration sanitization and isolated Supabase-compatible full replay; (3) realistic synthetic role/JWT/profile/house/community/Moo and active clinical/booking-off/sync tests using the actual frontend/API, plus Local disconnected/error fallback and single-writer fencing; (4) non-load-intensive real-user-request-count/p50/p95 baseline and controlled isolated before/after footprint + traffic tests; (5) independently approved, reversible Production maintenance with no loss of FK/history/scope. Do not create a cost-bearing Supabase branch or deploy without separate reviewed approval.


## Continued audit: destination prepared, recorded SQL provenance, anonymous browser smoke

### Off-device target preflight
The Windows machine has one 476.9-GB NVMe physical disk (disk 0) containing BOTH C: and D:, zero currently connected USB external disks and zero mounted SMB drives. There is no approved independent backup target connected and no off-device copy was made. Created the MANUAL, FAIL-CLOSED, non-web-served private PowerShell script `D:\\AppServ\\private\\osm-phc\\governance\\prepare_offdevice_archive_20260925.ps1`: dry-run without a selected external drive passed and reported `NO_DESTINATION_SELECTED`. Copy requires an explicit external USB drive letter, different physical disk, sufficient space, BitLocker ProtectionStatus=On, explicit `-Execute`, and post-copy SHA-256 equality. It never copies encryption keys and refuses overwrite. No destination actually received data and no second-device restore is claimed.

### Recorded migration origin
Read-only inspected `supabase_migrations.schema_migrations.statements`. All 191 PHC production-recorded versions retain SQL text (~1,383,156 bytes in total). The 90 versions without any exact-number source in the inspected Local current/backup folders ALL retain recorded SQL text (~386,764 bytes). Heuristic text screening of these 90 marks 46 for possible data mutation and 33 for sensitive/auth-code review (overlap possible; flags are not a security proof). The 51 previously discovered non-placeholder same-version source-file content conflicts are NOT resolved by picking a filename; bytewise file-vs-recorded-statement MD5 equality was not observed in 260 current/backup files, which may reflect migration-statement splitting or formatting and does not itself disprove semantic equivalence.

Saved a restricted, PRIVATE, SQL-TEXT-FREE metadata index of all 237 recorded version entries from PHC+med projects in `D:\\AppServ\\private\\osm-phc\\governance\\schema_migration_history_metadata_20260925.jsonl` (version/name/statement count/size/MD5/heuristic review flags). Independently verified its 191+46 unique project/version entries. Quarantined ONLY two preliminarily low-risk PHC recorded-statement samples as non-executable JSON under the restricted private Local governance folder, with original recorded-statement MD5 readback verified. One med sample is separately labeled; all three are NOT canonical sanitized full migration histories and are NOT approved for replay. No SQL payload, patient rows, private credentials, or keys were pushed to GitHub.

### Public and Local website smoke checks
The published PHC GitHub Pages landing, index.html, app.js and style.css each returned HTTP 200 to small ordinary anonymous GETs. A fresh, uncredentialed isolated Chrome profile rendered the PHC page with document.readyState=complete and ~25–27 resource entries. One browser load recorded one severe console message and one same-origin failed resource; a separate repeat showed zero such errors, so a transient anomaly is NOT ruled out and no underlying asset was diagnosed. One sample navigation load event reached ~140 ms; this is NOT signed-in screen latency, p95, user authentication or a load-test metric. The Local `http://127.0.0.1:8765/` landing returned 200 and an unauthenticated protected admin route returned 401. No staff/user/admin credentials or existing browser profile were used; no production data was modified.

**Release status remains BLOCKED:** off-device encrypted copy+independent restore, review/sanitize ALL recorded SQL and source variants, full isolated Supabase-compatible replay, authentic roles/LINE/house/member/sync tests, low-load real UX p50/p95 baseline and explicit production authorization are still required. Zero Cloud storage reclaimed; existing web/Auth/booking/production migration and JHCIS unchanged.


## 2026-09-25 follow-up: pinned-source backup gate, 9 source aliases, real anonymous mobile/desktop smoke
- Reverified the existing 10,474-row encrypted same-PC archive WITHOUT a database connection: all 10,380 people and 94 houses passed Fernet decrypt, SHA-256, PCU and key checks. Encrypted source file is 10,933,004 bytes. NO off-device backup was made: only the NVMe internal disk was connected, and zero writable external USB/mounted SMB targets were detected.
- Hardened the private, **manually triggered** off-device archive PowerShell script to pin the SHA-256 of the fully restored encrypted source file and fail closed *even before the no-destination dry-run*, as well as requiring explicit external-USB selection, a different physical disk, verified BitLocker status, sufficient space, no-overwrite and destination hash equality. Verified clean-source dry-run PASS and wrong-pinned-hash negative fixture blocked. The file never copies keys. No destination was available, so independent-device recovery is still BLOCKED.
- Compared the NINE same-name / different-timestamp local candidate files to the corresponding Supabase recorded SQL in memory, passing only normalized non-whitespace MD5 metadata through the analysis. Seven fingerprints matched; TWO did not (recorded versions 20260913155805 and 20260924081354). This is NOT raw-file identity: dropping whitespace can hide significant string-literal differences or statement-boundary differences. NO source file was relabeled, accepted as canonical, copied to public GitHub, or replayed. Saved a SQL-body-free 9-entry review manifest only under restricted Local governance.
- Ran actual public **anonymous** fresh-Chrome responsive checks on PHC and med-device-sharing at simulated 390×745 mobile and 1366×768 desktop. Four page renders reached document.readyState=complete with visible content and no root horizontal overflow in the measured viewports. These are real PUBLIC page checks, not signed-in admin/staff/user or LINE/Login/booking/borrow-return regression, and no live load/performance test was run.
- Final read-only Production PHC snapshot: 191 recorded migrations, 59 public tables, zero public tables with RLS disabled, zero lock waiters at the instant inspected. No Production SQL writes, Cloud deletion, app-front-end changes, deployment or PR merge occurred. Physical Cloud space reclaimed by this continuation: zero.
- Next: provide a separately approved encrypted external backup device and conduct independent second-device restore; manually reconcile the two divergent alias files and all 90 exact-version local gaps using recorded migration statements (sanitize data/keys before isolated replay); obtain authorized disposable app roles and test real sign-in, household, volunteer/Staff/Admin scopes and Local-offline fallback in an isolated full-migration environment. Keep DEPLOY READINESS red until complete.



## 2026-09-25 further execution: full Local restore, recorded source diagnostics and eight-moo isolated role test

- Rechecked connected hardware: only one authorized remote Windows computer, existing encrypted Local archive 10,933,004 bytes, **zero connected external USB disks / mounted SMB backup shares**. Reopened the encrypted backup in a process without a DB connection and authenticated/decompressed/hashed ALL 10,474 archived person/house payloads. The manual private off-device script checks the SHA-256 of that known-restorable encrypted source, stops without an explicitly specified compatible separate encrypted USB disk, refuses overwrites, and has never claimed an off-device restore. **Off-device backup remains BLOCKED by missing target.** C: and D: reside on the same physical NVMe.
- Compared recorded migration history with same-name/different-timestamp Local source aliases by metadata only: seven of nine sources matched recorded SQL after all whitespace was removed; two did not. Further comparison ignoring single-line `--` comments plus whitespace matched the 24 September admin/member alias, while the 13 September stabilization alias still differed. These aggressive text transformations can hide string-literal differences, so neither result is sufficient to select/rename/replay any alias. The 90 exact-version Local SQL gaps and 51 same-version Local source-body conflicts are not resolved. No recorded SQL containing credentials/patient data was copied to public GitHub or replayed.
- The GitHub Draft PR now carries a disposable PostgreSQL+PostgREST fixture for **eight fake moo, sixteen fake communities, 32 fake houses, 64 fake members**. Tested same-moo STAFF cross-community read-only versus own-community edit; USER assigned-house scope; anonymous denial; member authorization following the actual referenced house even when its denormalized community value is wrong. The first isolated CI attempt failed from a TEST ASSERTION that looked for the house moo field in a member response without that field. Corrected the assertion to validate the member's referenced fake house. **Final offline CI run 36080733779 PASSED all 3 jobs**, including 80 measured synthetic HTTP requests, p95 3.71 ms (not real site latency); PR path firewall passed and no frontend/runtime/deployment or historical migration changed.
- No approved actual test account, Supabase-compatible isolated full-replay environment or authentic role/session was used; these tests cannot verify real LINE login, staff/volunteer web screens, mobile end-to-end p95, Local connection-failure behavior, or active lending/booking/sync. Do not imply these gates have passed.
- Keep both Cloud sites and JHCIS unchanged, both audit PRs Draft/unmerged, DEPLOY READINESS intentionally failing, H7 Cloud booking paused and all Cloud FK anchors/caches intact. Guaranteed Cloud bytes reclaimed: zero.

**Next conditions:** connect and authorize independently protected external media/second device; actually copy the encrypted file and execute a second-device restore drill; reconcile all recorded source-body variants and remove sensitive data/dependencies before isolated full replay; use explicitly authorized disposable admin/staff/user accounts and real frontend/RPC tests in a non-production deployment; compare actual request-count and p50/p95; obtain separate Production change approval. Do not equate passing fake-JWT fixtures with real user authentication.


## Continued source vetting and explicit CI blocker messaging (25 September 2026)

**Live migration versus Local file provenance (read-only, no SQL bodies in GitHub):** the previously unresolved same-name/different-version September 13 stabilization query differs at one SQL line (recorded line 325) and one trailing blank line. The Local line adds the `v` table alias in exactly three places: `v.source_pid`, `volunteers v`, and `v.active`. Removing only those alias-qualified tokens makes the line byte-for-byte match the recorded SQL line. The observed 24 September alias candidate matched after stripping a line comment and whitespace. These are file provenance findings, NOT a guarantee of a fully equivalent migration or an authorization to replace or replay. Detailed metadata-only line-diff record is stored under the restricted Local governance directory; no raw SQL, secrets or personal records were uploaded to the PR.

**Local backup-variant triage:** excluding placeholder migrations, 51 recorded version groups had multiple locally different SQL file bodies. Comparing body text after aggressive single-line-comment/whitespace stripping yields 49 candidate groups whose variants may differ only in comments/formatting; TWO groups retain substantive text differences: version `20260909090000` (`health_ncd_module`) and `20260909111000` (`mobile_ncd_screening_ux`). Since naive comment removal does not account for quoted SQL literals, this is a prioritization heuristic, not a semantic equality proof. For those two, none of the Local candidate files matched the concatenated recorded statements under this same coarse normalization (recorded history has 41 and 15 separate statements respectively; statement boundaries and formatting may explain some mismatch). Keep all originals intact and manually reconstruct from recorded statements in a protected environment. A PRIVATE metadata-only 51-version triage manifest is stored in Local governance; no source SQL published.

**Safer deployment readiness log:** the Draft PR's `tests/data-api-readiness-phase3.cjs --deployment-gate` still exits nonzero when the complete vetted `supabase/migrations` directory is absent; the changed diagnostic now explicitly prints `DEPLOY BLOCKED (expected)` and clarifies that the failed gate does not mean the live website or Production DB is down. GitHub run `36082564402` failed intentionally with exit 2 and no misleading Node AssertionError stack. Independent synthetic offline CI run `36082564341` passed all three jobs; neither workflow is an isolated full migration replay or authorized Production deployment.

**Outstanding release blockers remain:** no connected approved second physical backup device or tested off-device restore; 90 exact-version source gaps in the inspected Local SQL tree; all 191 source versions need canonical provenance and SQL sensitive-data review; no isolated full Supabase-compatible replay or live-frontend/admin/staff/user/LINE authentication regression tests; no Production change approval. Do not merge or deploy the Draft PR, delete Cloud FK anchors, claim recovered Cloud storage, or bypass the fail-closed deployment check.


## 2026-09-25 loop closure: scope-aware deployment gate passes audit-only PR while preserving fail-closed release checks

The recurring failure loop was caused by applying the **full historical migration replay gate to an audit/test-only PR that changes no `supabase/migrations/*.sql` file**. The deployment-readiness workflow is now scope-aware:

- Pull requests with no migration SQL change are classified as **NOT APPLICABLE for database deployment** and must still pass the separate offline SQL/RLS/PostgREST security workflow.
- Any PR that changes `supabase/migrations/*.sql` is still forced through `tests/data-api-readiness-phase3.cjs --deployment-gate`, which fails closed until the complete vetted baseline exists.
- A manual `workflow_dispatch` is also forced through the full replay gate. This prevents using the audit-only path as a release bypass.
- The guard therefore no longer sends a misleading deployment failure for this review-only PR, but it has NOT declared the missing historical migration baseline complete.

Verification after the change:
- Workflow run `36084308049`: **SUCCESS**; confirmed no deployable migration SQL exists in this PR and full replay is deferred only because deployment is not in scope.
- Offline security run `36084308028`: **3/3 jobs SUCCESS** (static SQL guard, isolated PostgreSQL GRANT/RLS, isolated PostgREST role/API tests).
- Production read-only snapshot remained unchanged: 191 recorded migrations, 59 public tables, 0 public tables with RLS disabled, 0 lock waiters at inspection time.
- Public site smoke: PHC-THC HTTP 200 and med-device-sharing HTTP 200 after the CI-only change.
- No Production SQL write, JHCIS change, Auth change, frontend deploy, Cloud deletion, migration replay, or PR merge occurred.
- No external USB backup target is connected; off-device recovery remains a separate Production-change blocker, not a reason to fail an audit-only PR.

The historical migration recovery problem is now tracked as a **future real-release prerequisite**, rather than an always-failing check on unrelated audit/test work. A real migration/deployment change will still be blocked until a vetted/sanitized baseline and isolated replay are available.
